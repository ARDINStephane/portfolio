    <div class="input-group md-form form-sm form-2 pl-0">
        {{ form_start(form, { 'action': path('project_search'), 'method': 'GET', 'attr': {'class': 'form-inline my-2 my-lg-0'}}) }}
        {{ form_row(form.search, {'attr': {'class':'form-control my-0 py-3 lime-border'}}) }}

        <div class="input-group-append">
            <button class="btn btn-outline-secondary perso-search"><i class="fas fa-search" aria-hidden="true"></i></button>
        </div>
        {{ form_end(form) }}
    </div>
